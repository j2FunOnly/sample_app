<% content_for :title, 'All users' %>
<h1>All users</h1>
<ul class="users">
  <% @users.each do |user| %>
    <%= gravatar_for user, size: 52 %>
    <%= content_tag(:li, user.name) %>
  <% end %>
</ul>
